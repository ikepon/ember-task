<div class="row">
  <div class="col-sm-4">
    <ul>
      {{#each model as |task|}}
        <li>{{task.title}}:{{task.description}}:{{task.complete}}:{{task.deadline}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="col-sm-8">
    {{outlet}}
  </div>
</div>
